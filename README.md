# Jiahe Chen's Website 

## Run Locally 
Make sure `ruby` is installed. 
Run the following command to build and run the website. 
```bash
bundle install
bundle exec ./bin/jekyll build
bundle exec ./bin/jekyll serve
```

